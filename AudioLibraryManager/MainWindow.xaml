<Window x:Class="AudioLibraryManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:view="clr-namespace:AudioLibraryManager.View"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="MainWindow" 
        Height="500" Width="1150">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="510"/>
            <ColumnDefinition Width="410"/>
            <ColumnDefinition Width="210"/>
        </Grid.ColumnDefinitions>
        <view:TracksListView x:Name="TracksListView" Margin="2" Grid.Column="0"/>
        <view:AuthorsListView x:Name="AuthorsListView" Margin="2" Grid.Column="1"/>
        <view:GenresListView x:Name="GenresListView" Margin="2" Grid.Column="2"/>
    </Grid>
</Window>
